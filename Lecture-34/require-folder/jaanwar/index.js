// by default empty object {}


let cheeta = require('./cheeta') //object = {cheeta:fn}
let jaadu = require('./jaadu') //object = {jaadu:fn}
let sher = require('./sher') //object = {sher:fn}

module.exports = {cheeta ,jaadu , sher}